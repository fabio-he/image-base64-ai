# 图片AI检测客户端部署文档

## 环境要求
- Python版本：推荐 **Python 3.10.x**
> Python3.12/3.13 可以运行，但BeautifulReport依赖setuptools做distutils兼容；
> 不建议高于3.13，BeautifulReport已经停止维护。

## 项目文件清单
- main.py               主程序入口
- config.py             全部业务、接口、轮询配置
- requirements.txt      依赖库清单，一键安装

## 全新机器部署步骤
1. 安装Python，勾选 Add Python to PATH
2. 打开cmd，cd进入项目根目录
3. 创建虚拟环境
```bash
python -m venv .venv